# Issue 002 - Future GLB model

## Goal

Add optional GLB support for a future 3D model.

## Current implementation

The repository contains assets/models as a placeholder directory.

## Future work

Add Three.js viewer.

Load a local fictional Vorsia GT GLB model if present.

Provide fallback behavior when no model exists.

Add orbit controls and subsystem materials.

Keep large model files out of normal commits unless intentionally curated.
