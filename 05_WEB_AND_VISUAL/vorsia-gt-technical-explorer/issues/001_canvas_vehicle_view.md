# Issue 001 - Canvas vehicle view

## Goal

Improve the stylized vehicle rendering while keeping the site dependency-light.

## Current implementation

The canvas draws a side silhouette, wheels, cabin, aero streamlines and subsystem highlight zones.

## Future improvements

Add top-down mode.

Add exploded technical mode.

Add layer toggles.

Add reduced-motion option.

Add export screenshot button.

## Acceptance criteria

Canvas should remain readable and responsive.

The selected subsystem should be visually obvious.

The text panel should always explain the selected subsystem.
