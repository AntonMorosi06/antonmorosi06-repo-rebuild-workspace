# Browser test checklist

The page loads without console errors.

The hero section is visible.

The system selector buttons update the selected system.

The vehicle canvas draws the silhouette.

Telemetry values update over time.

The intensity slider changes telemetry behavior.

The aero balance slider changes downforce mode.

The reset telemetry button works.

The layout is usable on mobile width.

The fictional concept note is visible.
