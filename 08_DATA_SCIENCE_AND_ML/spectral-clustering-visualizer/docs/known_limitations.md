# Known limitations

The implementation uses dense matrices.

The eigen decomposition is educational and not optimized for large datasets.

There is no sparse matrix backend yet.

There is no animated step-by-step mode yet.

There is no CSV import yet.

There is no automatic eigen-gap cluster selection yet.

The visualizer is a learning tool, not a production clustering platform.
