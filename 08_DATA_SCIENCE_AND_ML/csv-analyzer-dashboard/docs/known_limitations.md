# Known limitations

The analyzer is dependency-light and not intended to replace pandas.

Type inference is heuristic.

Date parsing supports a limited set of formats.

Correlation is computed only for numeric column pairs.

Large files are loaded into memory.

The dashboard currently loads a fixed JSON report path.

There is no browser-side CSV upload yet.

There is no automatic chart generation yet.
