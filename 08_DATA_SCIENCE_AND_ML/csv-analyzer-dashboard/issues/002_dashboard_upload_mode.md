# Issue 002 - Dashboard upload mode

## Goal

Allow users to load a JSON report from the browser.

## Future improvements

Add file input for report JSON.

Add drag-and-drop report loading.

Add validation messages.

Add sample report selector.

## Acceptance criteria

The dashboard should still work as a static GitHub Pages site.
