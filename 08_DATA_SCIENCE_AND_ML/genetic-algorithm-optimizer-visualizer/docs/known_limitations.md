# Known limitations

The genetic algorithm is educational and not tuned for production optimization.

The genome is currently two-dimensional.

The fitness landscape rendering is cached but still simple.

There is no CSV export of history yet.

There is no step-by-step parent-child visualization yet.

There is no multi-objective optimization yet.

The Pygame interface is a learning tool, not a general optimization platform.
