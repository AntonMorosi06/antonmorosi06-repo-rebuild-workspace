# Known limitations

The DBSCAN implementation uses a direct O(n²) neighborhood search.

There is no KD-tree or spatial index yet.

There is no animated step-by-step mode yet.

There is no CSV import yet.

There is no export of clustering results yet.

The Pygame interface is educational and not a full data analysis application.

Large datasets may reduce performance because all pairwise distances are checked directly.
