# Asset strategy

The reconstructed baseline uses generated geometric shapes instead of external image assets.

This keeps the repository small and immediately runnable.

Future versions can add sprite sheets, explosion animations, sound effects, music and UI icons. If assets become large, they should be curated carefully and not dumped blindly into the repository.
