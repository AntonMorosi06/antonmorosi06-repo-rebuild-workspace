# Pygame optional note

Pygame is not required for the whole repository.

The core engine, text demo, curses mode and Tkinter mode can work without pygame.

Install pygame only when you want to run the Pygame implementation:

pip install -r requirements-pygame.txt

This keeps the baseline lightweight and avoids breaking the repository when optional graphics dependencies are not installed.
