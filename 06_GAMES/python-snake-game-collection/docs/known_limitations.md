# Known limitations

The project does not include advanced animations.

The Pygame version uses simple generated shapes, not external art assets.

The Tkinter version is intentionally basic.

The curses version depends on terminal compatibility.

There is no persistent high-score file yet.

There is no sound system yet.

The text mode is a deterministic demonstration, not a full interactive game.
