# Changelog

## v0.1.0 - Reconstructed AI vocal API stipendio baseline

Created a clean skeleton for AI Vocal API Stipendio inside the AntonMorosi06 repository rebuild workspace.

Added synthetic salary-profile dataset generation.

Added NumPy logistic regression classifier.

Added feature scaling and validation helpers.

Added model persistence to JSON.

Added CLI commands for generate-data, train, predict, status and serve.

Added FastAPI application with health, status, dataset, train and predict endpoints.

Added optional voice/TTS helper layer.

Added .env.example, model card, privacy note, dataset note, train-before-predict guide, curl examples, issues, labels and tests.

No clone from AntonMorosi2234 was attempted.
