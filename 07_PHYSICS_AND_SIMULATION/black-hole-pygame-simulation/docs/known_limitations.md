# Known limitations

The model is not physically accurate general relativity.

The renderer uses generated shapes rather than advanced shaders.

There is no WebGL or Three.js version in this repository yet.

There is no CSV telemetry export yet.

There is no configuration file for presets yet.

There is no UI menu yet.

The simulation may become heavy if particle counts are increased too much.
