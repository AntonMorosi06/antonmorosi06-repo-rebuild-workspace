# Known limitations

The simulation is not physically accurate.

The rendering uses Canvas 2D rather than WebGL.

The lensing is stylized, not real ray tracing.

There is no audio-reactive mode yet.

There is no preset export/import yet.

There is no automatic screenshot button yet.

Large particle counts may reduce performance on weaker devices.
